version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: gamex-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: gamex
    volumes:
      - mongodb_data:/data/db
    networks:
      - gamex-network

  mongo-express:
    image: mongo-express:latest
    container_name: gamex-mongo-express
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_URL: mongodb://mongodb:27017/
      ME_CONFIG_BASICAUTH: false
    depends_on:
      - mongodb
    networks:
      - gamex-network

volumes:
  mongodb_data:
    driver: local

networks:
  gamex-network:
    driver: bridge
